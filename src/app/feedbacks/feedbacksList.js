define(['react', 'lodash', './feedbacksList.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'feedbacksList',
        render: template
    });
});
