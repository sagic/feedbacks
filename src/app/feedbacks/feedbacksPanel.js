define(['react', 'lodash', './feedbacksPanel.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'feedbacks',
        render: template
    });
});
